
   Beispiel von Verwendung von bison && flex (zusammen).

   S. Makefile für eine Erklärung bzgl. Herkunft.

   S. test.sh für ein Shellskript, dass das Beispielprogramm testet.

   Geben Sie im Consolefenster einfach "make" ein, um das Programm zu bauen,
   bzw. "make tests" an, um das Programm nach Bedarf zu bauen und danach zu
   testen (es wird einfach das Shellskript durchgeführt).

   Ronald Moore
